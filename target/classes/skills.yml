fire_ball:
  name: "&c大火球"
  skills:
    #single skill
    fire_ball:
      #skill type
      type: shoot
      #adjusable attributes are varied by skill type
      projectile: fireball
      power: 3
      duration: 5
      triggers: right_click #should also can function if array is used
      targets:
      requirements:
        - "mana >= 5"
      cooldown: 10
      probability: 1
      costs:
        mana: 5